import { TableProps } from './table.types';
export declare const Table: <T extends Record<string, any>>({ columns, dataSource, loading, rowSelection, pagination, infiniteScroll, scrollThreshold, onLoadMore, hasMore, rowKey, stickyHeader, height, emptyText, variant, size, colors, onRow, className, style, }: TableProps<T>) => import("react/jsx-runtime").JSX.Element;
